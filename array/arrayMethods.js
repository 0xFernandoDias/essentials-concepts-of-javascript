const soccerPlayers = ['Neymar', 'CR7', 'Messi']

soccerPlayers.push('Mbappe') // it adds the string in the last position
soccerPlayers.unshift('Marta') // it adds in the first position

console.log(soccerPlayers)