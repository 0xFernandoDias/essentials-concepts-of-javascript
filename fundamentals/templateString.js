const name = 'Fernando'
const template = `Hello ${name} !` //this just concats, we gotta use ``
console.log(template)

//mathematical expressions
console.log(`1+1=${1+1}`)

//example of arrow function
const up = text => text.toUpperCase() // this is like: up = function up(text) {text.toUpperCase()}
console.log(`Hey... ${up('watch out')}!`)

