const school = "Home Idiomas"

console.log(school.charAt(3)) //this will show us which character is in the 3th position of the array

console.log(school.substring(1)) //this will print all letters from 1st of the array

console.log(school.concat(' School').concat('!')) //this is like " school + ' School' + '!' "

console.log(school.replace("o", "0")) //this will replace the first "o" to "0"

console.log('Fernando,Neuza,Carlos'.split(',')) //this will create a array separating strings with a comma
